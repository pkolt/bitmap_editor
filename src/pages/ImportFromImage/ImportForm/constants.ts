import { FormValues } from './types';

export const defaultValues: FormValues = {
  files: null,
  top: 0,
  left: 0,
  width: 128,
  height: 64,
  threshold: 100,
  name: '',
  invertColor: false,
};
